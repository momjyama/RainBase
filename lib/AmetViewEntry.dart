import 'package:flutter/material.dart';

class AmetViewEntry extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
        appBar: AppBar(
          title: const Text("Amet View Entry"),
        ),
        body: SafeArea(
          child: Text("Jai"),
        ));
  }
}
